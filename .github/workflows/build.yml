name: build

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write          # needed to attach binaries to a Release on tags

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-14
            label: macOS
          - os: windows-latest
            label: Windows
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-gui.txt

      - name: Core selftest
        run: python selftest.py

      - name: Build app
        run: pyinstaller --noconfirm MA2TimecodeCut.spec

      - name: Package macOS (.dmg)
        if: runner.os == 'macOS'
        run: |
          cd dist
          # hdiutil intermittently fails with "Resource busy" on hosted runners
          # (Spotlight/indexer touching the freshly written .app) — retry a few times.
          for i in 1 2 3 4 5; do
            hdiutil create -volname "MA2 Timecode Cut" \
              -srcfolder "MA2 Timecode Cut.app" -ov -format UDZO \
              "MA2-Timecode-Cut-macOS.dmg" && break
            echo "hdiutil attempt $i failed; retrying in 10s…"
            sleep 10
          done
          test -f "MA2-Timecode-Cut-macOS.dmg"

      - name: Package Windows (.zip)
        if: runner.os == 'Windows'
        run: |
          cd dist
          Compress-Archive -Path "MA2 Timecode Cut" -DestinationPath "MA2-Timecode-Cut-Windows.zip"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: MA2-Timecode-Cut-${{ matrix.label }}
          path: |
            dist/*.dmg
            dist/*.zip
          if-no-files-found: error

      - name: Attach to Release (on tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/*.dmg
            dist/*.zip
